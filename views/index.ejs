<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>sorry</title>

    <style>
        :root {
            --bg: #0f1224;
            --pink: #ff9ecd;
            --pink-light: #ffd6ea;
            --purple: #9b7cff;
            --glass: rgba(255, 255, 255, 0.08);
            --soft: rgba(255, 255, 255, 0.18);
            --text: #fdfdff;
            font-family: "Poppins", "Segoe UI", system-ui;
        }

        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            background: linear-gradient(135deg, #171a3a, #0f1224 60%, #1e1442);
            color: var(--text);
        }

        .wrap {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .card {
            width: min(720px, 95%);
            background: rgba(255, 255, 255, 0.06);
            border-radius: 20px;
            padding: 26px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(14px);
            box-shadow: 0 10px 45px rgba(255, 120, 180, 0.25);
            animation: fadeIn 1.3s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        header {
            display: flex;
            gap: 15px;
            align-items: center;
            flex-wrap: wrap;
        }

        .avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, #ff9ecd, #ff6fae);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            box-shadow: 0 6px 20px rgba(255, 90, 160, 0.4);
            animation: pulse 2s infinite ease-in-out;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        h1 {
            margin: 0;
            font-size: 22px;
            line-height: 1.3;
        }

        p.lead {
            margin-top: 6px;
            opacity: 0.85;
        }

        .names {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 10px;
        }

        .pill {
            padding: 8px 14px;
            border-radius: 50px;
            background: rgba(255, 255, 255, 0.08);
            border: 1px solid rgba(255, 255, 255, 0.15);
            font-weight: 600;
        }

        .message,
        .poem {
            margin-top: 16px;
            padding: 16px;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 14px;
            line-height: 1.6;
        }

        .poem {
            font-style: italic;
            color: var(--pink-light);
            text-align: center;
        }

        .buttons {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            margin-top: 22px;
        }

        button {
            padding: 12px 18px;
            border: 0;
            border-radius: 12px;
            cursor: pointer;
            font-weight: 700;
            font-size: 15px;
            transition: 0.2s;
        }

        .btn-forgive {
            background: linear-gradient(135deg, var(--pink), #ff6fae);
            color: white;
            box-shadow: 0 6px 25px rgba(255, 90, 150, 0.35);
        }

        .btn-forgive:hover {
            transform: scale(1.05);
        }

        .btn-no {
            background: transparent;
            border: 1px dashed var(--pink-light);
            color: var(--pink-light);
        }

        #noteInput {
            width: 100%;
            margin-top: 12px;
            padding: 12px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.08);
            border: 1px solid rgba(255, 255, 255, 0.15);
            color: white;
        }

        #sendNote {
            margin-top: 10px;
            padding: 12px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            width: 100%;
        }

        .heart {
            position: fixed;
            top: -10vh;
            width: 22px;
            height: 22px;
            opacity: 0.8;
            pointer-events: none;
            animation: fall linear infinite;
        }

        @keyframes fall {
            to {
                transform: translateY(120vh) rotate(360deg);
            }
        }

        @media(max-width:450px) {
            h1 { font-size: 18px; }
            .avatar { width: 65px; height: 65px; font-size: 26px; }
        }
    </style>
</head>

<body>

<div class="wrap">
    <div class="card">

        <header>
            <div class="avatar">💖</div>
            <div>
                <button id="touchMeBtn" class="btn-forgive" style="margin-top:15px;">🎵 Touch Me</button>
                <h1>Just something small, made only for you 💫</h1>
                <p class="lead">Home feels warmer when you are there — stay happy, stay you</p>
                <div class="names">
                    <div class="pill">Nandini</div>
                    <div class="pill">Vanshika</div>
                    <div class="pill">Parul</div>
                </div>
            </div>
        </header>

        <div class="message" id="mainMessage">
            <p>
                I know you are home right now, probably relaxing a bit.
                And honestly… I miss you. Not in a dramatic way,
                just that calm kind of missing where small things remind me of you.
            </p><br>

            <p>
                Some people talk nonsense, some judge without knowing anything.
                But none of that defines you. You are genuine, warm,
                and way too real for that noise.
            </p><br>

            <p>
                When you come back, you owe me handmade food 🍱,
                a big smile, and yes —
                a proper party for your new laptop and tablet 🎉.
            </p><br>

            <p>
                Please don’t eat too much unhealthy junk food.
                Take care of your health, stay fit, stay glowing.
                And take care of your family too.
                That matters a lot.
            </p><br>

            <p>
                Just be you. That’s already more than enough.
            </p>

            <i style="font-size: small;">
                (PS: Not written by any AI, just emotions, bad grammar and honesty.)
            </i>
        </div>

        <div class="message">
            <h3 style="margin-bottom:8px; color: var(--pink-light);">
                💌 Today’s Message — <span style="font-size:14px;">22 Jan 2026</span>
            </h3>
            <p>
                You are looking absolutely gorgeous in that saree.
                Couldn’t take my eyes off you.
            </p>
        </div>

        <div class="poem">
            Missing you, but not crying about it 😌 <br>
            Just waiting for your handmade food, stories,
            and that smile I like too much.
        </div>

        <div class="buttons">
            <button class="btn-forgive" id="forgiveBtn">💗 It’s okay, come talk</button>
            <button class="btn-no" id="noBtn">❌ Not today</button>
        </div>

        <form action="/message" method="post">
            <input id="noteInput" type="text" placeholder="Type your message" name="message">
            <input id="noteInput" type="text" placeholder="Enter your name" name="name">
            <button type="submit" id="sendNote">Send</button>
        </form>

    </div>
</div>

<audio id="bgMusic" loop autoplay>
    <source src="Jax - Like My Father (Official Video) [GCWl50HQZIM].mp3" type="audio/mpeg">
</audio>

<script>
    function rand(min, max) { return Math.random() * (max - min) + min; }

    for (let i = 0; i < 18; i++) {
        const h = document.createElement('div');
        h.className = 'heart';
        h.style.left = rand(0, 100) + 'vw';
        h.style.animationDuration = rand(6, 16) + 's';
        h.innerHTML = "💗";
        document.body.appendChild(h);
    }

    noBtn.onmouseenter = () => {
        const x = rand(20, window.innerWidth - 100);
        const y = rand(80, window.innerHeight - 100);
        noBtn.style.position = "absolute";
        noBtn.style.left = x + "px";
        noBtn.style.top = y + "px";
    };

    forgiveBtn.onclick = () => {
        mainMessage.innerHTML = "Thank you for giving me your time. Whenever you're ready, I’m here ❤️";
    };

    const bg = document.getElementById("bgMusic");
    let musicStarted = false;
    const touchBtn = document.getElementById("touchMeBtn");

    touchBtn.onclick = () => {
        if (!musicStarted) {
            bg.play().catch(() => {});
            musicStarted = true;
            touchBtn.style.display = "none";
        }
    };
</script>

</body>
</html>
